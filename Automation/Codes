Automation Codes

1) How to point the curser location
test.getDriver().switchTo().activeElement();

2) How to validate curser for the element
(test.getDriver().switchTo().activeElement()).equals(test.getDriver().findElement(Loc.ExecutionApproval));



